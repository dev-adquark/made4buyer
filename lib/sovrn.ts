import type{DealCandidate}from"./types";
function s(v:unknown){return typeof v==="string"?v.trim():""}
function n(v:unknown){const x=typeof v==="number"?v:Number(v);return Number.isFinite(x)?x:undefined}
export async function findDeals(productName:string):Promise<DealCandidate[]>{const base=process.env.SOVRN_API_URL;if(!base)return[];const url=base+(base.includes("?")?"&":"?")+"product="+encodeURIComponent(productName);const headers:Record<string,string>={Accept:"application/json"};if(process.env.SOVRN_API_KEY)headers.Authorization="Bearer "+process.env.SOVRN_API_KEY;const r=await fetch(url,{headers,cache:"no-store"});if(!r.ok)throw new Error("Sovrn "+r.status);const d=await r.json();const items=Array.isArray(d)?d:(d.offers||d.items||d.results||d.data||[]);const seen=new Set<string>();return items.map((x:any)=>({productName,merchant:s(x?.merchant)||s(x?.retailer)||s(x?.seller)||undefined,dealId:s(x?.id)||s(x?.dealId)||undefined,url:s(x?.url)||s(x?.link)||s(x?.affiliateUrl),price:n(x?.price??x?.currentPrice),currency:s(x?.currency)||undefined,availability:s(x?.availability)||s(x?.stock)||undefined,source:"sovrn"})).filter((x:DealCandidate)=>{if(!x.url||!/^https?:\/\//i.test(x.url))return false;const key=(x.dealId||x.url).toLowerCase();if(seen.has(key))return false;seen.add(key);return true}).sort((a,b)=>(a.price??Number.POSITIVE_INFINITY)-(b.price??Number.POSITIVE_INFINITY)).slice(0,10)}