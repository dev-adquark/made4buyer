import{NextResponse}from"next/server";import{validAdmin,sign,adminSessionMaxAge}from"@/lib/auth";
export async function POST(req:Request){const f=await req.formData();const email=String(f.get("email")||"");const password=String(f.get("password")||"");if(!validAdmin(email,password))return NextResponse.redirect(new URL("/admin/login?error=1",req.url));const res=NextResponse.redirect(new URL("/admin",req.url));const issuedAt=String(Date.now());const payload=issuedAt+"."+email;res.cookies.set("admin_session",payload+"."+sign(payload),{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge:adminSessionMaxAge});return res}