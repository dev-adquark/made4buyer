import {db} from "@/lib/db"; import {NextResponse} from "next/server";
const allowed=new Set(["page_view","affiliate_click","compare","search"]);
export async function POST(req:Request){try{const b=await req.json();const event=String(b.event||"");if(!allowed.has(event))return NextResponse.json({ok:false,error:"Unsupported event"},{status:422});const reviewId=typeof b.reviewId==="string"&&b.reviewId?b.reviewId:undefined;const category=typeof b.category==="string"&&b.category?b.category.slice(0,120):undefined;const sessionId=typeof b.sessionId==="string"&&/^[a-zA-Z0-9_-]{10,100}$/.test(b.sessionId)?b.sessionId:undefined;await db.analyticsEvent.create({data:{reviewId,event,category,sessionId,metadata:typeof b.metadata==="object"&&b.metadata?b.metadata:undefined}});return NextResponse.json({ok:true})}catch{return NextResponse.json({ok:false},{status:400})}}
