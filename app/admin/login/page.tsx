import {redirect} from"next/navigation";import{isAdmin}from"@/lib/auth";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){if(await isAdmin())redirect("/admin");const q=await searchParams;return <main className="section"><div className="container"><div className="review" style={{maxWidth:460,margin:"auto"}}><h1>Admin login</h1>{q.error&&<p className="warn" role="alert">Invalid email or password.</p>}<form action="/api/admin/login" method="post"><input name="email" type="email" placeholder="Email" autoComplete="username" required style={{width:"100%",padding:12,marginBottom:12}}/><input name="password" type="password" placeholder="Password" autoComplete="current-password" required style={{width:"100%",padding:12,marginBottom:12}}/><button className="btn primary" type="submit">Sign in</button></form></div></div></main>}