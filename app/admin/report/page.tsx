import {db} from "@/lib/db"; import {isAdmin} from "@/lib/auth"; import {redirect} from "next/navigation";
export const dynamic="force-dynamic";
export default async function Report(){if(!await isAdmin())redirect("/admin/login");const [ingested,published,deals,verified,clicks]=await Promise.all([db.ingestionRun.aggregate({_sum:{total:true}}),db.review.count({where:{status:"PUBLISHED"}}),db.deal.count(),db.deal.count({where:{verified:true}}),db.analyticsEvent.count({where:{event:"affiliate_click"}})]);return <main className="admin"><div className="container"><h1>Day 30 Success Report</h1><div className="stats"><div className="stat">Ingested<b>{ingested._sum.total||0}</b></div><div className="stat">Published<b>{published}</b></div><div className="stat">Deal coverage<b>{deals?Math.round(verified/deals*100):0}%</b></div><div className="stat">Affiliate clicks<b>{clicks}</b></div></div><section className="section"><p className="muted">This report uses recorded platform data. Search Console indexing percentages require the production Search Console integration and are intentionally not fabricated.</p></section></div></main>}